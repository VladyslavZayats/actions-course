name: First Workflow
on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "echo a string step" 
      - name: Multiline Command
        run: |
          python3 --version
          node -v